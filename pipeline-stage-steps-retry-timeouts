pipeline {
    agent any 
    stages {
        stage('buil'){
            steps {
                retry (3) {
                    echo "before throwing error"
                    echo "error in  retry"
                }
            }    echo "after retry(3)"
        }
    }
}
