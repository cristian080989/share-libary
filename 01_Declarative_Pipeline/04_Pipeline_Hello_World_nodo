pipeline{
    agent{
        node{
            label "docker_maven"
            customWorkspace "/home/jenkins/customworkspace"
        }
    }
    stages{
        stage('build'){
            steps{
                echo 'Hello World'
            }
        }
    }
}
